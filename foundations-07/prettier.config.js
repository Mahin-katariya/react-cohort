module.exports = {
  // Use the Tailwind Prettier plugin to sort classnames automatically
  plugins: [require('prettier-plugin-tailwindcss')],
  singleQuote: true,
  semi: true,
  trailingComma: 'es5',
  tabWidth: 2,
  printWidth: 80,
  arrowParens: 'avoid',
};
